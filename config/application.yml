development:
  aws:
    access_key_id: 'key'
    secret_access_key: 'secret'

test:
  aws:
    access_key_id: 'key'
    secret_access_key: 'secret'

production:
  aws:
    access_key_id: 'key'
    secret_access_key: 'secret'